{
  "name": "cypress-helloworld-tryout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --headed --browser chrome",
    "dev": "cypress open",
    "publish": "node ./publish.js",
    "init_dev": "yarn --dev && yarn dev",
    "init_test": "yarn --dev && yarn test",
    "gitUpdate": "git add . && git commit -m\"update cypress-xpath-tryout,\" && git pull && git push",
    "trim": "rm -rf node_modules",
    "prettier_format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "latest",
    "cypress-xpath": "^2.0.1",
    "prettier": "latest"
  }
}
