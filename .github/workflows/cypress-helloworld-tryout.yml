name: master-cypress-helloworld-tryout

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: test using docker compose
        run: |
          cd cypress-helloworld-tryout
            npm run into_cy_chrome
          cd ..
